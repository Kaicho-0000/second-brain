`00_Inbox/` の未整理ノートを処理する。確認不要、即実行。

1. `00_Inbox/` 内のすべての `.md` ファイルを読み込む
2. 各ノートについて:
   - 内容を分析し、最適な移動先フォルダを決定する
   - フロントマターを整備する（tags, created, status を付与）
   - 関連する既存ノートを検索し `[[wikilink]]` を追加する
   - ファイルを移動先へ移動する（`mv` で即実行）
   - ファイル名が命名規約に沿っていなければリネームする
3. 空になった場合は完了メッセージを表示
4. 処理結果を一覧で報告する:
   ```
   処理完了:
   - old-name.md → 30_Tech_Notes/new-name.md
   - memo.md → 20_Projects/project-name.md
   ```
